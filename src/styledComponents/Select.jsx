import { styled } from "@/styles/stitches.config";

const Select = styled("select", {
  border: "none",
  outline: "none",
  bg: "$gray200",
  p: "$1 $4",
  borderRadius: "$md",
});

export default Select;
